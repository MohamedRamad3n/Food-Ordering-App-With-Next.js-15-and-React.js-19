"use client"

import { Translations } from "@/types/translations"
import { Session } from "next-auth"

const EditUserForm = ({transilation,user}:{translations:Translations,user:Session["user"]}) => {
    
  return (
    <div>
      
    </div>
  )
}

export default EditUserForm
